# MySQL (required: URL must start with mysql:// — not file: or postgresql://)
# Aiven MySQL + Windows TLS: keep sslaccept=... and set MYSQL_TLS_INSECURE=true for local dev (see src/utils/prisma.js).
DATABASE_URL="mysql://avnadmin:AVNS_JRiRumBiEeydyOwV1x4@rm-database-lloyola129105-ea9f.i.aivencloud.com:18272/defaultdb?sslaccept=accept_invalid_certs"
MYSQL_TLS_INSECURE=true

# JWT
JWT_SECRET=apartment-mgmt-super-secret-key-change-in-production-2024
JWT_EXPIRES_IN=7d

# Server
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Stripe (optional)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
